import React from 'react';
import ReactDOM from 'react-dom';
import { FactoriaApp } from './FactoriaApp';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <FactoriaApp />
  </React.StrictMode>,
  document.getElementById('root')
);
